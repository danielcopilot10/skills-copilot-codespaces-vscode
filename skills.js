
function addNumbers(num1, num2) {
    return num1 + num2;
}


function calculateGravityOfPlanet(mass, radius) {
    return (6.674 * Math.pow(10, -11)) * (mass / Math.pow(radius, 2));
}

function calculateforce(mass, acceleration) {
    return mass * acceleration + 2;
}